{
  "version": 2,
  "buildCommand": "cd ClientApp && rm -f package-lock.json && npm install --legacy-peer-deps && node scripts/build-vercel.js && cross-env NODE_OPTIONS=--max-old-space-size=4096 npm run build:prod",
  "outputDirectory": "ClientApp/dist/sdms-b2c-webapp",
  "installCommand": "cd ClientApp && rm -f package-lock.json && npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}

